from collections import deque

class MovingAverage:
    def __init__(self, k):
        self.q = deque()
        self.k = k

    def next(self, val):
        self.q.append(val)
        if len(self.q) > self.k:
            self.q.popleft()
        return sum(self.q) / len(self.q)
