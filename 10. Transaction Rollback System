class Transaction:
    def __init__(self):
        self.stack = []

    def execute(self, action, rollback):
        action()
        self.stack.append(rollback)

    def undo(self):
        if self.stack:
            self.stack.pop()()
